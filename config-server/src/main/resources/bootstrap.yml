spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nomad-coder/mmssaa.git
          search-paths:
            - configs
server:
  port: 8888